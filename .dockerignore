# Dependencies (rebuilt inside container)
node_modules
server/node_modules
client/node_modules

# Build artifacts (rebuilt in Docker stages)
client/dist

# Database (use Docker volume)
server/data
server/*.db
*.db

# Dev / editor
.git
.gitignore
*.md
.vscode
.idea
*.log

# OS
.DS_Store
Thumbs.db

# Secrets and local recovery copies
.env
.env.*
!.env.example
server/gcp-tts-key.json
**/*.bak
**/*.before-*
